import { expect } from 'chai';

describe('Mock Test', () => {
  describe('First thing to test', () => {
    it('should be able to test something', () => {
      expect(5).to.equal(4+1);
    });
  });
});